---
## Problem Details
Title: "2352. Equal Row and Column Pairs"
Difficulty: "Medium"
Primary Topic: "Hash Table"
Tags: ["Array", "Hash Table", "Matrix", "Simulation"]
Time Complexity: O(n²)
Space Complexity: O(n)
---

## Intuition

Used a Hash Table to track the rows and their count and then matched them with column to get the result.

---

## Solution

```java
class Solution {
    public int equalPairs(int[][] grid) {
        int n = grid.length;
        Map<String, Integer> map = new HashMap<>();

        for (int i = 0; i < n; i++) {
            StringBuilder sb = new StringBuilder();
            for (int j = 0; j < n; j++) {
                sb.append(grid[i][j]);    
                sb.append(',');    
            }
            String s = sb.toString();
            map.put(s, map.getOrDefault(s, 0) + 1);
        }

        int count = 0;

        for (int i = 0; i < n; i++) {
            StringBuilder sb = new StringBuilder();
            for (int j = 0; j < n; j++) {
                sb.append(grid[j][i]);    
                sb.append(',');    
            }
            String s = sb.toString();
            count += map.getOrDefault(s, 0);
        }

        return count;
    }
}
}
```
